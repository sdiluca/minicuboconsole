import { SortByStrenghtPipe } from './sort-by-strenght.pipe';

describe('SortByStrenghtPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByStrenghtPipe();
    expect(pipe).toBeTruthy();
  });
});
